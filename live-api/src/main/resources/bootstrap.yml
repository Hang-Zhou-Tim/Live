# bootstrap is system-level configuration files, while application is user-level. So, it is load first and does not replace by any local settings.
# It is typically used for registering configurations for distributed server/database/microservices.
spring:
  application:
    name: live-api
  cloud:
    nacos:
      username: hang
      password: ZH131314
      discovery:
        server-addr: hang.nacos.com:8848
        namespace: 2257b780-fbd7-4424-a51f-7f3445885412 # Here we can only design the namespace id
      config:
        import-check:
          enabled: false
        file-extension: yaml
        server-addr: hang.nacos.com:8848
        namespace: 2257b780-fbd7-4424-a51f-7f3445885412
  config:
    import:
      - optional:nacos:live-api.yaml
