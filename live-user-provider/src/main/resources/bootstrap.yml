# bootstrap is system-level configuration files, while application is user-level. So, it is load first and does not replace by any local settings.
# It is typically used for registering configurations for distributed server/database/microservices.
spring:
  application:
    name: live-user-provider
  cloud:
    nacos:
      username: hang
      password: ZH131314
      discovery:
        server-addr: 47.95.42.71:8848
        namespace: 6a80b005-82be-4051-b86f-9daf116e9e9d # Here we can only design the namespace id
        heart-beat-interval: 1000
        heart-beat-timeout: 3000
      config:
        import-check:
          enabled: false
        # Find the suffix ending with yaml.
        file-extension: yaml
        # Read Nacos Address
        server-addr: 47.95.42.71:8848
        # Read config nacos namespace
        namespace: 6a80b005-82be-4051-b86f-9daf116e9e9d
        timeout: 5000
        max-retry: 3
  config:
    import:
      - optional:nacos:live-user-provider.yaml